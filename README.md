# Sample project with Laravel Mix

- Create virtual host 'local.test' and point to /public dir. If domain is different than 'local.test', change the same in webpack.mix.js
- Run `yarn` and then `yarn watch`
